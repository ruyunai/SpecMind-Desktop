# 当前状态

## 当前焦点
**v0.2.0 Release 已发布 ✅** -- GitHub Release v0.2.0 exe 上传完成，下载链路验证通过（HTTP 200，130.52 MB）。README.md 「立即下载」按钮自动指向新版 exe。累计修复 BUG-001 ~ BUG-030（3 项致命 + 17 项高危），v0.2.0 包含 BUG-023 ~ BUG-030 全部修复（exe 打包 LLM 静默回退 / 高风险弹窗滚动 / ChromaDB 遥测 / Agent 输出解析优化）。

## 项目里程碑进度

```
✅ M4  前端深度打磨        (A-F 6 项全部完成)
✅ M5  GUI 真实环境验证      (Qt 事件循环 + 完整链路)
✅ M10 PyInstaller 打包      (130.52MB 单文件 exe，含 certifi+chromadb 全量)
✅ M6.1 安全加固 (S1-S4)     (密钥/审计/清理/日志轮转)
✅ M6.2 可观测性 (O1-O4)     (metrics/追踪/JSON 日志)
✅ M6.3 Eval 回归 (E1-E3)    (pytest CI 5/5)
✅ M7   DeepSeek LLM 接入    (7 个 Agent 全部真实调用 + LLM 失败显式标注)
✅ M8.1 U 盘便携模式         (portable.dat 检测)
✅ M8.2 知识库自行上传       (解析→分块→嵌入→入库全链路)
✅ M-Bug 全量 Bug 修复       (BUG-001 ~ BUG-030，3 致命 + 17 高危全部清零)
⬜ M9   知识库管理增强       (多维筛选/过期标记/批量操作)
⬜ M11  代码签名 + 自动更新
⬜ M12  Legal Agent 联网搜索  (Tavily/GLM-4 web_search，用户暂不实现)
```

## 最近变更

- 2026-07-14：**v0.2.0 Release 发布** ✅
  - GitHub Release v0.2.0 标签：exe (130.52 MB) 上传完成
  - 下载链路验证：HTTP 200，Content-Length=136860380 字节
  - README.md 更新 v0.2.0 版本历史 + 文件大小 + 下载链接自动跳转
  - Release 说明含 BUG-023 ~ BUG-030 全部修复清单
  - 记忆文件 + README commit `85c8c0b` 已推送 master

- 2026-07-14：**exe 打包 LLM 静默回退修复** ✅（BUG-030 致命）
  - 3 层连锁失败：SSL 证书缺失 + API Key 路径不同 + 静默回退 mock
  - spec 添加 certifi/openai/httpx 数据文件 + hiddenimports
  - state.py 新增 llm_errors 字段（Annotated List reducer）
  - mock_agents.py LLM 失败不再静默，mock 数据标注 `⚠ LLM 调用失败`
  - orchestrator.py 完成后检查 llm_errors 并发警告日志
  - exe 重新构建 130.52 MB

- 2026-07-14：**高风险阻断弹窗滚动问题修复** ✅（BUG-029）
  - BUG-029: Interrupt 阻断对话框内容过长无法滚动 → 新建 InterruptConfirmDialog 自定义 QDialog
  - 三区布局：标题区(固定) + 合规问题详情(QTextEdit 可滚动) + 按钮区(固定底部)
  - 窗口尺寸限制 400×600 ~ 700×800，鼠标滚轮可滚动查看全部合规问题
  - 底部「拒绝放行」和「强制放行」按钮始终可见，不再被挤出可视区
  - 涉及文件：src/gui/dialogs/interrupt_dialog.py（新建）, src/gui/main_window.py

- 2026-07-14：**人工测试发现 7 项 Bug 全部修复** ✅（BUG-023 ~ BUG-028）
  - BUG-023: ChromaDB 上传报 posthog 模块缺失 → 禁用遥测修复
  - BUG-024: 左栏资产库缺「历史PRD」「通用文档」分类 → 补充分类 + 修复枚举映射
  - BUG-025: docx/pdf 表格解析缺失 → docx 加表格提取 + pdf 换 pdfplumber
  - BUG-026（致命）: LLM 只参考企业资产库 → 全部 Prompt 重写为 LLM 知识为主 + 资产库为辅
  - BUG-027: 配套计划交付计划看不到 → workspace.py int.replace 崩溃修复
  - BUG-028: 输出有 JSON 残留 → markdown 清理 + dict 提取 + JSON 解析
  - 核心架构变更：confidence.py Legal 空检索不再强制阻断，由 LLM 判定风险
  - 7 个 Agent Prompt 全文已发用户审核
  - 新增依赖：pdfplumber==0.11.10

- 2026-07-14：**稳定性测试 + 详细部署手册** ✅
  - 新建 `tests/test_stability_core.py`：6 大类 54 项核心功能检查（上传/删除/分类/线程终止/BUG 回归），首次运行全部通过
  - 重写 `docs/deploy.md` 为完整部署操作手册（12 节 + 2 附录）

- 2026-07-14：**全量 Bug 修复 + AGENTS.md 标准化** ✅
  - 扫描 39 项 Bug（致命 2 / 高危 12 / 中危 14 / 低危 11）
  - 修复 BUG-009 ~ BUG-022 共 14 项（致命 + 高危全部清零）：
    - BUG-009 Legal 空检索强阻断不可被 LLM 覆盖
    - BUG-010 workspace.py Path 未导入致 3 个导出按钮全失效
    - BUG-011/012 prompts.py 转义字符 `\\n` + 字段名 feature_tag/tag 不一致
    - BUG-013 llm_client.py System/User 消息拆分逻辑错（HumanMessage 为空）
    - BUG-014 mock_agents Planner LLM 输出未解析为列表
    - BUG-015 Contract RAG 功能点列表取空
    - BUG-016 exporter.py 周数解析硬编码 replace("周","") 崩溃
    - BUG-017 config.py Agent 配置加载 except:pass 吞错
    - BUG-018 builder.py checkpointer SQLite 连接泄漏
    - BUG-019 orchestrator.py elapsed_ms 恒近零（stream_mode=updates 滞后）
    - BUG-020 main_window.py closeEvent 无法终止 QThread（cancel + wait + terminate 三层策略）
    - BUG-021 asset_library.py 枚举值 standard_feature vs feature 失配
    - BUG-022 upload_dialog.py 主线程同步执行 ingest_document 致 UI 冻结（QThread+Worker 模式）
  - 建立 `AGENTS.md` 标准 AI 代理工作指南（12 节），沉淀项目结构/规范/约束/禁止事项
  - 每项 Bug 修复后单独 git commit + push，修复日志同步更新

- 2026-07-13：**企业知识库上传功能全部完成** ✅
  - 上传管线：`gui/services/upload_service.py` — parse → chunk → embed → store 全链路
  - 上传 GUI：`gui/dialogs/upload_dialog.py` — 文件选择/分类/进度/结果显示
  - 资产库集成：`asset_library.py` 新增「上传」按钮，上传后自动刷新
  - 去重机制：hash 去重，重复上传自动跳过 6/6 条
  - 字符串兼容修复：`chroma_store.py` / `retriever.py` 三处 `category` 参数兼容 string/enum
  - 文档管理：`chroma_store.py` 新增 `delete_by_source()` 方法
  - e2e 验证：上传 6 块→RAG 检索 3 条→Agent 7 节点工作流 → 风险=low，全部通过 ✅
  - 企业现在可以：打开 GUI → 点「上传」→ 选自己的 Word/PDF/TXT → Agent 从企业文档中检索

- 2026-07-13：**部署文档 + 使用说明** ✅
  - `docs/deploy.md`：三种部署方式（exe / U盘便携 / 源码）+ FAQ
  - `docs/usage.md`：界面概览 / 7 步工作流详解 / 模型配置 / 成本参数 / 数据安全

- 2026-07-13：**U 盘便携模式** ✅
  - `core/__init__.py`：`is_portable()` / `get_app_root()` — 检测 exe 同级 `portable.dat`
  - `config.py` + `crypto.py`：统一使用 `get_app_root()` 替代硬编码 `%APPDATA%`
  - 便携模式下所有数据存储在 exe 同级目录，拔插 U 盘即迁移

- 2026-07-13：**DeepSeek LLM 接入** ✅
  - 新建 `src/agents/llm_client.py`：统一 ChatOpenAI 管理，按 Agent 路由模型
  - config.py 改为 DeepSeek 默认（base_url = api.deepseek.com，model = deepseek-chat）
  - SAR/Legal/Contract RAG Agent 接入 LLM；PM/Review/Planner Mock Agent 升级为 LLM 调用
  - Commercial Agent 保持公式驱动（预算可控，不依赖 LLM）
  - 容错：所有 Agent LLM 调用包裹 try/except，失败自动回退 mock
  - e2e 回归：7 节点 68s，10/11 字段对齐

- 2026-07-13：**阶段 8.6 安全加固全部完成** ✅
  - S1 密钥派生：随机 seeds + uuid.getnode() 机器绑定 + PBKDF2 + 旧格式迁移
  - S2 审计日志：audit_logs 表 run_id/elapsed_ms + 3 索引，16 条/次记录
  - S3 过期清理：retention=90 天，写入时附带节流清理
  - S4 日志路径：RotatingFileHandler，5MB×3，兼容 frozen 模式

- 2026-07-13：**阶段 8.4 可观测性** ✅
  - O1 节点耗时信号：`node_finished(str, int)` 携带 elapsed_ms
  - O2 Metrics 查询：`get_node_timing_stats()` / `get_workflow_summary()`
  - O3 LLM 追踪：run_id 全链路贯通
  - O4 JSON 结构化日志：`_StructuredFormatter` + frozen 模式自动启用

- 2026-07-13-14：**RAG 重构 + 联调修复** ✅
  - BM25 trigram 迁移：unicode61 → trigram，Recall@5 0.94 → 1.00
  - P0/P1 修复 5 项：builder rag_agents / import_btn 信号 / client_name 输入 / asset_library 懒加载 / checkpointer 绝对路径
  - Contract FTS5 语法冲突修复 / RiskLevel 反序列化消除
  - GUI 真实环境 e2e：7 节点 10.99s，PRD/报价/交付计划正确显示

- 2026-07-13：**报价逻辑灵活化** ✅
  - CostConfig dataclass + 公式驱动报价 = 不同项目不同价格
  - GUI Ctrl+, → 成本参数 Tab，参数可调 + 保存

## 当前架构概览

```
GUI (PySide6, 三栏深色主题)
 ├─ 左：资产库（ChromaDB 企业文档，支持上传）
 ├─ 中：工作区（需求输入 → PRD → 附件导出）
 └─ 右：工作流画布（7 节点状态 + 拓扑图）

Orchestrator (LangGraph + QThread)
 ├─ SAR Agent   → RAG(标准功能库) → LLM → 需求清洗
 ├─ Legal Agent → RAG(法规库)     → LLM → 合规预检
 ├─ PM Agent    → LLM → PRD 生成
 ├─ Commercial  → 公式计算 → 双报价（不调用 LLM）
 ├─ Contract    → RAG(合同模板库) → LLM → 条款比对
 ├─ Review      → LLM → 三维评审
 └─ Planner     → LLM → 交付计划

存储层
 ├─ ChromaDB   → 向量检索（企业上传的文档）
 ├─ SQLite     → FTS5 BM25 + 审计日志 + 资产 FTS
 └─ 文件系统    → 日志 (JSON Lines + RotatingFileHandler)

安全
 ├─ 密钥加密   → Fernet + PBKDF2 + 机器绑定
 └─ API Key   → secrets.enc 加密存储
```

## 已知问题

1. **检索相似度偏低**（非阻塞）：上传文档少时，RAG 相似度 <0.6 触发降级提示。这是正常设计——企业文档越多，检索越精准。
2. **FTS5 LIKE 回退噪音**（性能无影响）：中文分词无 jieba 依赖，trigram 主路径召回率足够，LIKE 回退仅用于 <3 字符短 token，日志 WARNING 可忽略。
3. **stream_mode 信号滞后**（P2 架构限制）：LangGraph 0.2.x updates 模式在节点返回后才 yield，GUI "执行中"状态短暂。
4. **exe 未代码签名**：SmartScreen 会拦截，发布给用户前需要签名。

## 下一步建议

1. **中危 Bug 修复**（P2）：BUG-023 ~ BUG-036 共 14 项中危问题（如配置项校验、错误提示细节、日志格式统一等），可在后续迭代中分批处理
2. **知识库管理增强**（P1）：资产库增加「删除文档」右键菜单、过期标记可视化、多选批量操作
3. **代码签名 + 自动更新**（P1）：购买代码签名证书 + 版本检查机制
4. **API Key 验证按钮**（P2）：Ctrl+, 对话框中增加"测试连接"按钮
5. **成本控制**（P2）：token 计数 + 每 Agent 预算 + 超限阻断（需 LLM 运行时数据积累）
