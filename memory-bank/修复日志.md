# 修复日志

> 每次 Bug 修复逐条记录，不覆盖。AI 遇同类 Bug 必须先查此日志，禁止重复尝试已失败方案。

---

<!-- 模板：复制以下结构追加新 BUG 记录

## BUG-XXX: {{标题}}
- **现象**：{{表现}}
- **根因**：{{根因分析}}
- **修复方案**：{{改了哪些文件/函数/逻辑}}
- **是否成功**：✅ 成功 / ❌ 失败（附失败原因）
- **失败后替代方案**：{{第二次用了什么方案}}
- **涉及文件**：{{文件路径列表}}
- **日期**：{{日期}}
-->

## BUG-001: langgraph 0.2.34 缺少 interrupt() 和 Command
- **现象**：`ImportError: cannot import name 'interrupt' from 'langgraph.types'`
- **根因**：`interrupt()` 和 `Command` 在 langgraph 0.3.x 才引入，0.2.34 不可用
- **修复方案**：放弃原生 interrupt()，改用条件路由 + resume 图 + threading.Event 模式
  - 主图 Legal 高风险 → 路由到 END（阻断）
  - Orchestrator 发 workflow_blocked 信号，MainWindow 弹确认框
  - 用户确认 → Orchestrator 运行独立 resume 图（PM → fan-out → Planner）
  - 用户拒绝 → workflow_complete 终止
- **是否成功**：✅ 成功
- **涉及文件**：src/graph/builder.py, src/core/orchestrator.py, src/gui/main_window.py
- **日期**：2026-07-13

## BUG-002: 并行节点 InvalidUpdateError "Can receive only one value per step"
- **现象**：Commercial/Contract/Review 并行执行时报 `InvalidUpdateError: At key 'raw_input': Can receive only one value per step`
- **根因**：并行节点返回完整 state（含 raw_input 等相同字段），LangGraph 不允许同一 super-step 多个节点写同一字段
- **修复方案**：
  1. 重构 mock_agents.py：所有节点只返回本节点更新的字段（部分 dict），符合 LangGraph 规范
  2. 并行节点（Commercial/Contract/Review）不返回 `current_node`（会冲突）
  3. state.py 的 `audit_snapshots` 改用 `Annotated[List[Dict], operator.add]` reducer，支持并行追加合并
- **是否成功**：✅ 成功
- **涉及文件**：src/agents/mock_agents.py, src/agents/state.py
- **日期**：2026-07-13

## BUG-003: audit_snapshots 计数只有 1（应为 7）
- **现象**：工作流完成后日志显示「审计快照数: 1」，实际执行了 7 个节点
- **根因**：Orchestrator 用 `graph.get_state(config)` 从 checkpointer 拉取最终 state 覆盖本地 state，但 checkpointer 在并行场景下 audit_snapshots 可能不完整（reducer 行为不符预期）
- **修复方案**：在 `_run_main_graph` 的 `get_state` 后，对比手动累加的 audit_snapshots 数量与 checkpointer 的数量，取较大值
  ```python
  manual_snapshots = self._state.get("audit_snapshots", [])
  self._state = dict(final_state.values)
  if len(manual_snapshots) > len(self._state.get("audit_snapshots", [])):
      self._state["audit_snapshots"] = manual_snapshots
  ```
- **是否成功**：✅ 成功（验证脚本确认 7 个节点快照全部记录）
- **涉及文件**：src/core/orchestrator.py
- **日期**：2026-07-13
- **备注**：升级 langgraph-checkpoint-sqlite 到 3.1.0 后，checkpointer 本身的 audit_snapshots 也变为 7，但修复逻辑保留作为向后兼容保护

## BUG-004: ChromaDB metadata 不接受嵌套 dict
- **现象**：seed_data.py 加载报错 `ValueError: Expected metadata value to be a str, int, float, bool, SparseVector, list, or None, got {...} which is a dict`
- **根因**：`make_meta()` 返回的 AssetMeta 含 `extra` 字段（嵌套 dict，如 `{"article_no": "第十三条", "keywords": [...]}`），ChromaDB metadata 仅支持扁平结构
- **修复方案**：在 `ChromaStore.add_documents()` 写入前调用 `_flatten_meta()` 平铺元数据
  - `extra` 字段提到顶层
  - 复杂类型（嵌套 dict）序列化为 JSON 字符串
  - list 元素为基本类型时保留原样
- **是否成功**：✅ 成功（19 条种子数据全部入库）
- **涉及文件**：src/storage/chroma_store.py
- **日期**：2026-07-13

## BUG-005: 存储路径相对路径导致 seed/eval 指向不同目录
- **现象**：seed_data.py 加载完数据后，test_rag_eval.py 仍报「集合为空」，Recall@5=0.00
- **根因**：ChromaStore/SqliteStore 默认路径 `data/chroma` 和 `data/specmind.db` 是相对路径，cwd 不同时指向不同位置
  - seed 在 `src/` 运行 → 写入 `src/data/`
  - eval 在项目根运行 → 读取 `项目根/data/`（空）
- **修复方案**：默认路径改为基于 `Path(__file__).resolve().parent.parent.parent / "data"` 的绝对路径
  ```python
  _DEFAULT_PERSIST = str(Path(__file__).resolve().parent.parent.parent / "data" / "chroma")
  _DEFAULT_DB = str(Path(__file__).resolve().parent.parent.parent / "data" / "specmind.db")
  ```
- **是否成功**：✅ 成功（无论 cwd 在哪里都指向项目根 data/）
- **涉及文件**：src/storage/chroma_store.py, src/storage/sqlite_store.py
- **日期**：2026-07-13

## BUG-006: doc_hash 丢失导致 RRF 合并所有结果为 1 条
- **现象**：向量召回 7 条，但 RRF 融合后只剩 1 条，Recall@5=0.06
- **根因**：`make_meta()` 默认返回 `doc_hash=""`（空字符串），`add_documents()` 中 `if "doc_hash" not in meta` 判断永远为 False（键存在但值为空），导致所有文档的 doc_hash 都是 ""，RRF 把它们当作同一条文档合并
- **修复方案**：把判断改为 `if not meta.get("doc_hash")`（空字符串也触发覆盖）
- **是否成功**：✅ 成功（RRF 后保留 5 条独立结果，Recall@5=0.94）
- **涉及文件**：src/storage/chroma_store.py
- **日期**：2026-07-13

## BUG-007: FTS5 unicode61 tokenizer 对中文短语匹配失效（BM25 全 0 召回）
- **现象**：20 条评估用例 BM25 召回全部 0 条，MATCH '人脸识别' / MATCH '数据出境' / MATCH '终身免费' 均无命中
- **根因**：`tokenize='unicode61'` 把中文按单字切分，「在线教育」会被切成「在/线/教/育」4 个单字 token，这些单字在文档中普遍存在（"的/在/一"等高频字），BM25 几乎无法区分相关性
- **修复方案**：迁移到 `tokenize='trigram'`（3 字符滑窗切分）
  1. `_init_tables` 改用 trigram tokenizer + 自动迁移逻辑（检测旧表 DROP + CREATE + 回填）
  2. `add_asset` 写入 FTS5 时把 metadata.keywords 拼接到 content 后（原 keywords 字段未进索引）
  3. `search_fts` 按 query 长度分流：
     - <3 字符：trigram 不支持，走 LIKE 模糊匹配
     - 3-5 字符：trigram MATCH（切成 1-3 个 3-gram AND，命中率高）
     - >=6 字符：按 4 字符滑窗切成多个子串，每段 trigram MATCH，结果 OR 合并（避免长 token 切成过多 3-gram AND 全失败）
- **是否成功**：✅ 成功
  - 单关键词召回：8/9 命中（仅 "BI" 2 字符走 LIKE 回退）
  - 评估 BM25 召回率：14/20 (70%)（从 0% 提升）
  - **Recall@5: 0.94 -> 1.00**（混合检索协同效应提升）
  - Prompt 安全性 1.00 / 置信度阻断 1.00 保持
- **失败后替代方案**：未尝试 jieba 预分词，trigram + 滑窗已达成 100% Recall@5
- **涉及文件**：src/storage/sqlite_store.py
- **日期**：2026-07-13
- **备注**：未采用 jieba 方案，理由：(1) 60MB 词典依赖违背本地优先原则 (2) PyInstaller bundle 复杂 (3) trigram 已达成 100% Recall@5，无升级必要

## BUG-008: builder.py checkpointer 相对路径导致 e2e 测试找不到数据
- **现象**：端到端测试中 builder.py 的 `SqliteSaver.from_conn_string(Path("data") / "checkpoints.db")` 在不同 cwd 下指向不同目录，与 ChromaStore/SqliteStore 的绝对路径不一致
- **根因**：builder.py 中 checkpointer 路径使用 `Path("data")`（相对路径），而 chroma_store.py 和 sqlite_store.py 已修复为项目根绝对路径
- **修复方案**：改为 `Path(__file__).resolve().parent.parent.parent / "data"` 与其他存储层保持一致
  ```python
  _checkpoint_dir = str(Path(__file__).resolve().parent.parent.parent / "data")
  ```
- **是否成功**：✅ 成功（与 chroma_store/sqlite_store 统一使用项目根绝对路径）
- **涉及文件**：src/graph/builder.py
- **日期**：2026-07-13

## BUG-009: Legal Agent 空检索阻断机制被 LLM 输出绕过（致命）
- **现象**：法规库检索结果为空（`confidence_level == "empty"`）时，`should_block_workflow` 返回 True 应阻断工作流，但若 LLM 调用成功并返回"低风险"，`legal_blocked` 被设为 False，工作流继续生成 PRD
- **根因**：`rag_agents.py` L173-201 中，`should_block` 变量在 LLM 成功分支被彻底忽略，仅在 LLM 失败的 else 分支（L217-220）才使用
- **修复方案**：在 LLM 成功分支末尾追加 `if should_block:` 判断，强制将 `legal_blocked = True` 且 `risk_level = RiskLevel.HIGH`，并记录 WARNING 日志
- **是否成功**：✅ 成功
- **涉及文件**：src/agents/rag_agents.py
- **日期**：2026-07-14
- **备注**：直接违反项目硬约束"Legal 高风险必须阻断 PRD 生成"和 `should_block_workflow` 设计意图，属致命级安全机制失效

## BUG-010: workspace.py 三个导出方法 Path 未导入致 NameError（致命）
- **现象**：点击「导出 Markdown / Word / JSON」三个按钮 100% 抛 `NameError: name 'Path' is not defined`，被 except 捕获弹"导出失败"，核心用户功能完全不可用
- **根因**：`Path` 仅在 `_on_import`（L242）内部做了 `from pathlib import Path` 局部导入，模块顶层无该导入；`_on_export_md`（L415）、`_on_export_word`（L433）、`_on_export_json`（L451）均调用 `Path(file_path)` 但各自作用域内不存在 `Path`
- **修复方案**：在 workspace.py 顶部添加 `from pathlib import Path` 模块级导入
- **是否成功**：✅ 成功
- **涉及文件**：src/gui/widgets/workspace.py
- **日期**：2026-07-14
- **备注**：现有测试 test_exporter.py 仅测 exporter 模块本身，未覆盖 workspace UI 导出方法，属"测试盲区 + 致命 bug"组合

## BUG-011: prompts.py Legal/Review Prompt 中 `\\n` 产生字面量反斜杠 n（高危）
- **现象**：PM Prompt 中 `legal_context = "\\n".join(items)` 拼接后多条法规问题用字面量 `\n` 文本分隔而非真实换行；Review Prompt 中 `features_text += ...\\n` 同样问题
- **根因**：Python 中 `"\\n"` 是 2 字符（反斜杠+n），而非 `"\n"`（真实换行符）。开发者混淆了转义写法
- **修复方案**：将 prompts.py 中所有 `"\\n"` 改为 `"\n"`（共 2 处：L180 Legal Context join + L215 Review features_text 追加）
- **是否成功**：✅ 成功
- **涉及文件**：src/agents/prompts.py
- **日期**：2026-07-14

## BUG-012: prompts.py Review Prompt 功能点 key 名错误（高危）
- **现象**：Review Prompt 中 `f.get('feature_tag', '?')` 恒返回 `'?'`，LLM 无法获取功能点的标准/定制/不支持分类信息，评审质量严重下降
- **根因**：功能点字典实际 key 为 `'tag'`（见 state.py FeatureTag 枚举 + mock_agents.py L234），但 Review Prompt 用了错误的 `'feature_tag'`
- **修复方案**：将 `f.get('feature_tag', '?')` 改为 `f.get('tag', '?')`；同时修正 PM Prompt L197 的字段描述 `feature_tag/dependencies` → `tag/dependencies` 并补充取值说明
- **是否成功**：✅ 成功
- **涉及文件**：src/agents/prompts.py
- **日期**：2026-07-14
- **备注**：与 BUG-011 同次修复（同一文件相邻行，同类字面量 + key 不一致问题）

## BUG-013: llm_client.py 无 User 分隔符时 HumanMessage 为空（高危）
- **现象**：当 prompt 以 "System:" 开头但不含 "\n\nUser:" 分隔符时，三元表达式 `(prompt, "")` 返回 2 元素元组，`len(parts) > 1` 恒为 True，导致 system = 整个 prompt，user = "" 空字符串，LLM 收到空用户消息
- **根因**：`parts = prompt.split(...) if ... else (prompt, "")` 的 else 分支设计错误，2 元素元组使后续 `len(parts) > 1` 判断失效；第 105-106 行的 else 是死代码永远不会执行
- **修复方案**：改为 `if prompt.startswith("System:") and "\n\nUser:" in prompt:` 同时满足两条件才拆分；用 `system_part, user_part = prompt.split("\n\nUser:", 1)` 解构赋值，避免 len 判断
- **是否成功**：✅ 成功
- **涉及文件**：src/agents/llm_client.py
- **日期**：2026-07-14
- **备注**：当前 build_*_prompt 均不以 "System:" 开头，此分支未被触发，属隐患修复

## BUG-014: mock_agents.py Planner 复用 _parse_llm_features 解析交付计划（高危）
- **现象**：Planner Agent LLM 调用成功后 `delivery_plan = _parse_llm_features(llm_reply)`，但 `_parse_llm_features` 回退逻辑返回 `[{"name", "tag", "desc"}]`，key 与交付计划所需 `phase`/`duration`/`deliverable` 完全不匹配
- **根因**：复用了为功能点列表设计的解析函数，未针对交付计划字段结构适配
- **修复方案**：新增 `_parse_llm_delivery_plan(llm_text)` 函数，规范化 key（deliverables→deliverable，weeks→duration），回退时构造单阶段占位结构；顺便修复 L530 冗余的 `replace("周","").replace("周","")` 为单次 replace
- **是否成功**：✅ 成功
- **涉及文件**：src/agents/mock_agents.py
- **日期**：2026-07-14

## BUG-015: rag_agents.py Contract RAG 从 PRD 字典取功能点恒为空（高危）
- **现象**：`_extract_contract_query_text` 中 `features = prd.get("功能点标注", prd.get("prd_features", []))` 永远返回空列表，因为功能点存储在 State 顶层 `prd_features` 字段，PRD 字典内不含此 key
- **根因**：误以为功能点列表存在 PRD 字典内，实际 state.prd 只含 8 模块文本或 {"llm_output": ...}
- **修复方案**：函数签名改为 `_extract_contract_query_text(prd, prd_features=None)`；在 `contract_agent_rag` 中从 `state.get("prd_features", [])` 取功能点并传入
- **是否成功**：✅ 成功
- **涉及文件**：src/agents/rag_agents.py
- **日期**：2026-07-14

## BUG-016: exporter.py duration/weeks key 不匹配 + int.replace 崩溃（高危）
- **现象**：导出 MD/Word 时 `weeks = int(phase.get("duration", "0周").replace("周", ""))`，但 Planner Prompt 要求 LLM 输出 `weeks` 而非 `duration`，导致总工期恒为 0；若 LLM 返回 `"duration": 3`（int），`3.replace("周", "")` 抛 `AttributeError`
- **根因**：导出器硬编码读 `duration` key 且假设值为 str 类型，与 Planner Prompt 输出规范（`weeks: N` int）不一致；与 BUG-014 构成完整 Planner 输出 → 解析 → 导出链路 bug
- **修复方案**：新增 `_extract_weeks(phase)` 和 `_format_duration(phase)` 辅助函数，兼容 duration/weeks key 与 int/str/"N周"/"约4周" 等多种格式（用正则提取数字）；MD/Word 两处导出均改用新函数
- **是否成功**：✅ 成功
- **涉及文件**：src/core/exporter.py
- **日期**：2026-07-14
- **备注**：与 BUG-014 同属 Planner 输出链路 key 不匹配问题，建议 Prompt/解析/导出三处统一为 duration（str "N周"）规范

## BUG-017: config.py 部分 Agent 配置缺失时静默丢弃 + 异常吞错（高危）
- **现象**：`agents_data` 含部分 Agent key（如旧配置只有 5 个）时，字典推导生成 5 条目的 dict（truthy），`or _default_agents()` 不触发，结果丢失 2 个 Agent 配置；后续 `cfg.agents.get(agent_key)` 对缺失 Agent 返回 None，Agent 静默使用默认模型而非用户配置。同时 `except (JSONDecodeError, KeyError, TypeError): pass` 静默吞掉所有异常无日志
- **根因**：`or` 短路仅在字典为空时触发兜底；部分缺失场景未考虑；except pass 无任何日志记录
- **修复方案**：以 `_default_agents()` 为底，逐个 Agent 用文件配置覆盖（缺失则保留默认），单 Agent 字段不匹配时 WARNING 但不中断；except 改为 `_config_logger.warning` 记录异常
- **是否成功**：✅ 成功
- **涉及文件**：src/core/config.py
- **日期**：2026-07-14

## BUG-018: builder.py SqliteSaver SQLite 连接泄漏（高危）
- **现象**：`build_graph` 中 `conn = sqlite3.connect(...)` 创建的连接传给 `SqliteSaver(conn)` 后，连接对象从未被关闭；函数返回后 conn 引用仅存于局部作用域，应用退出时 WAL 未 checkpoint、数据未刷盘
- **根因**：缺少模块级 conn 引用和关闭机制
- **修复方案**：新增模块级 `_checkpoint_conn` 保存连接引用；新增 `close_checkpointer()` 函数关闭连接；`build_graph` 重建时先关闭旧连接；`main.py` 在 `app.aboutToQuit` 连接 `close_checkpointer` 应用退出时自动清理
- **是否成功**：✅ 成功
- **涉及文件**：src/graph/builder.py, src/main.py
- **日期**：2026-07-14

## BUG-019: orchestrator.py elapsed_ms 测量恒为近零（高危）
- **现象**：`stream_mode="updates"` 在节点执行完毕后才 yield 事件，`entry_ts` 在事件接收时设置，`elapsed = int((time.time() - entry_ts) * 1000)` 测量的是"事件接收→State 合并→审计写入"耗时（通常 <1ms），而非节点实际执行耗时。审计日志和 GUI 显示的节点耗时全部接近 0ms，完全失去参考价值
- **根因**：LangGraph 0.2.x updates 模式天然滞后，节点开始时无法触发信号；耗时测量点错误
- **修复方案**：
  1. 修改 `_make_snapshot(node_name, start_time=None)` 接收可选 start_time，节点内部计算 elapsed_ms
  2. 在 mock_agents.py 和 rag_agents.py 的 11 个节点函数开头添加 `start_time = time.time()`，传入 `_make_snapshot`
  3. orchestrator 新增 `_extract_elapsed_ms(node_update, node_name)` 方法，从 audit_snapshots 提取节点内部测量的 elapsed_ms，回退到 stream 耗时
  4. 主图和 resume 图两处 stream 循环均改用新方法
- **是否成功**：✅ 成功
- **涉及文件**：src/agents/mock_agents.py, src/agents/rag_agents.py, src/core/orchestrator.py
- **日期**：2026-07-14
- **备注**：修复后 elapsed_ms 反映节点真实执行耗时（含 LLM 调用时间），GUI 和审计日志的耗时统计恢复正常

## BUG-020: main_window.py closeEvent 无法终止 QThread（高危）
- **现象**：窗口关闭时仅调用 `self._orchestrator.quit()` + `wait(3000)`，但 QThread.run() 内部正阻塞在 `graph.stream(...)` 上（等待 LLM 返回或处于 resume 图执行中），`quit()` 对自定义 run() 的 QThread 无效，3 秒后线程仍在运行，进程无法正常退出；若正处于 Interrupt 等待 `_confirm_event.wait()`，线程永远不会退出
- **根因**：`quit()` 仅对事件循环型 QThread 有效（退出事件循环），自定义 run() 不响应；`stream()` 迭代器无法被外部中断；`_confirm_event.wait()` 阻塞无法被 quit 唤醒
- **修复方案**：三层终止策略
  1. orchestrator 新增 `_cancel_flag` 标志位和 `cancel()` 方法，cancel() 同时：(a) 设置 `_cancel_flag=True`，(b) 设置 `_reject_flag=True`，(c) `_confirm_event.set()` 解除 Interrupt 阻塞
  2. 主图和 resume 图两处 `for event in graph.stream(...)` 循环开头检查 `if self._cancel_flag: break`，让 stream 自然退出
  3. main_window.py closeEvent 改为调用 `cancel()` → `wait(3000)` → 若仍运行则 `terminate()` 强制终止（兜底）
- **是否成功**：✅ 成功
- **涉及文件**：src/core/orchestrator.py, src/gui/main_window.py
- **日期**：2026-07-14
- **备注**：terminate() 是 Qt 提供的强制线程终止（操作系统级），仅作为兜底；正常情况下 cancel() + stream 循环检查即可让线程优雅退出

## BUG-021: asset_library.py AssetCategory 枚举值不匹配（高危）
- **现象**：左栏「企业资产库」点击「标准功能清单」展开时报错 `ValueError: 'standard_feature' is not a valid AssetCategory`，子节点显示「⚠ 加载失败: 'standard_feature' is not a valid AssetCategory」，标准功能库无法浏览
- **根因**：`asset_library.py` 的 `CATEGORY_GROUPS` 元组中第二项硬编码为 `"standard_feature"`，但 `storage/schema.py` 中 `AssetCategory.STANDARD_FEATURE.value = "feature"`（与 REGULATION/CONTRACT_TEMPLATE/PRD_HISTORY/COST_MODEL 一样使用短名）。`AssetCategory(cat_key)` 构造时按值匹配，找不到 `"standard_feature"` 抛 ValueError
- **修复方案**：将 CATEGORY_GROUPS 中标准功能清单的 key 从 `"standard_feature"` 改为 `"feature"`，与其他分类保持一致（regulation/contract/feature 都是短名）
- **是否成功**：✅ 成功
- **涉及文件**：src/gui/widgets/asset_library.py
- **日期**：2026-07-14
- **备注**：建议后续将 CATEGORY_GROUPS 的 key 直接改为引用 `AssetCategory.XXX.value`，避免硬编码字符串再次失配；当前修复保持改动最小化

## BUG-022: upload_dialog.py 上传主线程同步执行 UI 冻结（高危）
- **现象**：用户点击「上传到知识库」后，主线程直接执行 `ingest_document()`（含文档解析、按法条/条款分块、嵌入向量化、ChromaDB 入库），整个过程可能耗时数秒到数十秒（PDF/Word 较大时），期间整个应用 UI 完全冻结：进度条不动、无法拖动窗口、无法点击取消、Windows 还会显示「未响应」
- **根因**：`_on_upload` 在 Qt 主线程同步调用 `ingest_document()`，长耗时 I/O + CPU 操作阻塞事件循环
- **修复方案**：QThread + Worker QObject 模式异步化
  1. 新增内部 `_UploadWorker(QObject)` 类，含 `finished(object)` / `error(str)` 信号，`run()` 方法调用 `ingest_document`
  2. `_on_upload` 改为创建 Worker + QThread，`moveToThread` 后启动，立即返回（不阻塞 UI）
  3. 新增 `_on_upload_finished(result)` 和 `_on_upload_error(err_msg)` 回调，主线程处理结果/异常
  4. 新增 `closeEvent` 清理后台线程，防止关闭对话框时线程泄漏
- **是否成功**：✅ 成功
- **涉及文件**：src/gui/dialogs/upload_dialog.py
- **日期**：2026-07-14
- **备注**：修复后上传过程中 UI 保持响应，可拖动窗口、可点击关闭按钮；进度条仍为不确定进度（ingest_document 不返回中间进度），但至少 UI 不再冻结

## BUG-023: ChromaDB 上传报 No module named 'chromadb.telemetry.product.posthog'（高危）
- **现象**：用户上传文档到企业资产库时弹出错误 `No module named 'chromadb.telemetry.product.posthog'`，上传流程被中断，文档无法入库
- **根因**：`chroma_store.py` 第 39 行 `chromadb.PersistentClient(path=persist_path)` 未传入 `Settings` 配置，chromadb 1.5.9 默认启用遥测（posthog 产品分析），但该版本中 `chromadb.telemetry.product.posthog` 子模块路径变化或缺失，导致 ImportError
- **修复方案**：创建 PersistentClient 时显式禁用遥测
  ```python
  from chromadb.config import Settings
  self._client = chromadb.PersistentClient(
      path=persist_path,
      settings=Settings(anonymized_telemetry=False),
  )
  ```
- **是否成功**：✅ 成功（端到端上传测试通过：解析→分块→入库→删除 全流程无报错，日志显示 `telemetry=off`）
- **涉及文件**：src/storage/chroma_store.py
- **日期**：2026-07-14
- **备注**：此修复同时满足项目「禁止云端协作/上报依赖」硬约束——chromadb 遥测默认上报匿名使用数据到 posthog，禁用后完全本地化。源码模式重启应用即生效；exe 模式需重新构建 exe

## BUG-024: 左栏资产库缺「历史PRD」「通用文档」分类（高危）
- **现象**：上传对话框能选择 5 种分类（regulation/contract/prd/feature/generic），但左栏资产库只显示 3 种（feature/regulation/contract），「历史PRD」和「通用文档」上传后在左栏看不到
- **根因**：`asset_library.py` 的 `CATEGORY_GROUPS` 只定义了 3 个分类，缺少 prd 和 generic；同时 `schema.py` 的 `AssetCategory` 枚举缺 `GENERIC` 成员，`upload_service.py` 的 `CATEGORY_MAP` 把 generic 错误映射到 `STANDARD_FEATURE`
- **修复方案**：
  1. `schema.py` 新增 `GENERIC = "generic"` 枚举成员
  2. `asset_library.py` CATEGORY_GROUPS 补充「历史 PRD」和「通用文档」两个分组
  3. `upload_service.py` CATEGORY_MAP 把 generic 映射改为 `AssetCategory.GENERIC`
- **是否成功**：✅ 成功
- **涉及文件**：src/storage/schema.py, src/gui/widgets/asset_library.py, src/gui/services/upload_service.py
- **日期**：2026-07-14
- **备注**：人工测试发现迭代。CATEGORY_GROUPS 的 key 必须严格等于 AssetCategory.XXX.value

## BUG-025: docx/pdf 表格横线解析内容缺失（高危）
- **现象**：上传含表格的 Word/PDF 文档时，表格内容、横线分隔的内容大量丢失，但合同条款（纯文本）解析正常
- **根因**：`doc_parser._parse_docx` 只提取 `doc.paragraphs`，不提取 `doc.tables`；`_parse_pdf` 用 PyPDF2 基础 extract_text，对复杂布局支持差
- **修复方案**：
  1. docx: 用 lxml 遍历 `doc.element.body` 子元素，按文档原始顺序交替提取段落（w:p）和表格（w:tbl），表格转 Markdown 格式
  2. pdf: 从 PyPDF2 换用 pdfplumber（0.11.10），支持 extract_text + extract_tables，表格转 Markdown
  3. 新增 `_table_rows_to_markdown` 通用函数，表格行列表转 Markdown 表格文本
- **是否成功**：✅ 成功（10 项验证全部通过，表格转 Markdown 测试通过）
- **涉及文件**：src/parsers/doc_parser.py, requirements.txt
- **日期**：2026-07-14
- **备注**：人工测试发现迭代。安装了 pdfplumber==0.11.10 新依赖（用户确认允许）。docx 表格按文档顺序提取，不是先段落后表格

## BUG-026: LLM 只参考企业资产库，资产库没写的都敢做（致命）
- **现象**：Legal Agent 只看企业法规库，资产库没禁止的 LLM 什么都敢做，完全没有了违法顾虑。用户要求 LLM 自身有丰富的法律知识，应作为主要参考源
- **根因**：`prompts.py` 中 Legal/Contract/SAR 的 prompt 明确写"仅基于上述检索到的法规作答，不要使用检索结果以外的法条"——这是错误的 RAG 强约束设计，把 LLM 当成了无知识的检索器
- **修复方案**：
  1. 重写 SAR/Legal/Contract 三个 Prompt：改为"以你的法律/合同/产品知识为首要参考，企业资产库作为补充参考"
  2. 修改 `confidence.py` 的 `should_block_workflow`：Legal 空检索不再自动阻断，由 LLM 用自身知识判定风险
  3. 修改 `rag_agents.py` Legal Agent：解析 LLM 返回的 JSON 结构（risk_level + legal_issues），空检索时 LLM 正常作答
  4. 保留安全网：LLM 调用失败 + 空检索 → 才阻断（无法评估风险）
- **是否成功**：✅ 成功
- **涉及文件**：src/agents/prompts.py, src/agents/confidence.py, src/agents/rag_agents.py
- **日期**：2026-07-14
- **备注**：人工测试发现迭代。核心架构理念变更：从"RAG 强约束（防幻觉）"改为"LLM 知识为主 + RAG 补充"。覆盖了 BUG-009 的空检索强阻断逻辑

## BUG-027: 配套计划里交付计划看不到内容（高危）
- **现象**：工作流执行完成后，中栏「配套附件」Tab 里的交付计划区域为空白，但完整导出的文件里能看到交付计划
- **根因**：`workspace.py` L378 `int(phase.get("duration", "0周").replace("周", ""))` —— LLM 返回的 duration 是 int 类型（如 3），int 没有 `.replace` 方法，抛 AttributeError 被外层 try-except 吞掉，plan_text 为空
- **修复方案**：
  1. 在 workspace.py 顶部新增 `_extract_weeks(phase)` 和 `_format_duration(phase)` 安全解析函数
  2. `_extract_weeks` 兼容 int/float/str/带「周」字格式，用正则提取数字
  3. `_format_duration` 格式化工期显示文本
  4. 交付计划渲染改用这两个函数，空数据时显示"（无交付计划数据）"
- **是否成功**：✅ 成功
- **涉及文件**：src/gui/widgets/workspace.py
- **日期**：2026-07-14
- **备注**：人工测试发现迭代。此 bug 与 BUG-016 同源（exporter.py 已修但 workspace.py 漏修），现已统一用安全解析函数

## BUG-028: 输出内容有 `- { "tech": [` 等 JSON 残留（高危）
- **现象**：工作流输出内容里偶尔出现 `- { "tech": [`、`- [` 等 JSON 结构片段，不是很干净
- **根因**：
  1. LLM 输出 JSON 时带 markdown 代码块标记（```json ... ```），mock_agents 的 `find("[")` 提取不到
  2. Review Agent 的 LLM 返回 `{"tech": [...], ...}` dict 格式，但 mock_agents 直接截取前 300 字符作为文本显示
  3. PM Agent 的 LLM 返回 `{"prd": {...}, "prd_features": [...]}` dict，但 `_parse_llm_features` 只查找 `[...]` 数组
- **修复方案**：
  1. 新增 `_clean_markdown(text)` 函数：去除 ```json 和 ``` 标记
  2. 新增 `_extract_json(text)` 通用函数：先清理 markdown，再尝试直接解析/提取 dict/提取 list
  3. `_parse_llm_features` 支持从 dict 中提取 prd_features 字段
  4. PM Agent 优先解析完整 JSON 结构（prd + prd_features）
  5. Review Agent 解析 LLM 返回的 JSON 结构，提取 tech/design/qa/review_pass
  6. Legal Agent（rag_agents.py）也用 _extract_json 解析 risk_level + legal_issues
- **是否成功**：✅ 成功
- **涉及文件**：src/agents/mock_agents.py, src/agents/rag_agents.py
- **日期**：2026-07-14
- **备注**：人工测试发现迭代。所有 Prompt 新增"不要包含 markdown 代码块标记"指令 + 代码层双保险清理

## BUG-029: Interrupt 阻断对话框内容过长无法滚动（高危）
- **现象**：输入高风险销售数据时弹出阻断确认框，但框太长一口气显示全部内容，无法缩小或滚动查看，底部的"是否强制放行"和确认按钮被挤出可视区域看不到
- **根因**：`main_window.py` 用 `QMessageBox.question()` 系统原生对话框，不支持滚动条，内容过多时窗口被撑满屏幕，按钮挤出可视区
- **修复方案**：
  1. 新建 `src/gui/dialogs/interrupt_dialog.py` 自定义 `InterruptConfirmDialog(QDialog)`
  2. 布局分三区：标题区（固定）+ 合规问题详情（QTextEdit 可滚动）+ 按钮区（固定底部）
  3. 窗口尺寸限制：最小 400×600 / 最大 700×800，避免撑满屏幕
  4. 详情区用 QTextEdit 只读 + 深色主题样式，支持鼠标滚轮滚动
  5. 按钮区固定底部：「✗ 拒绝放行（推荐）」+「⚠ 强制放行」
  6. `main_window.py` 移除 QMessageBox 导入，改用 InterruptConfirmDialog
- **是否成功**：✅ 成功（导入验证通过）
- **涉及文件**：src/gui/dialogs/interrupt_dialog.py（新建）, src/gui/main_window.py
- **日期**：2026-07-14
- **备注**：人工测试发现迭代。QMessageBox 不适合长内容场景，自定义 QDialog + QTextEdit 是 PySide6 长内容对话框的标准模式

## BUG-030: exe 打包后 LLM 调用失败静默回退 mock 数据（致命）
- **现象**：exe 打包后输入任何需求都瞬间输出毫不相关的测试阶段内容，无延迟、无进程，与源码模式完全脱节
- **根因**：3 层连锁失败：
  1. SSL 证书缺失：spec 未打包 certifi cacert.pem，exe 中 HTTPS 请求失败
  2. API Key 路径不一致：源码模式存 `项目根/config/app.json`，exe 读 `%APPDATA%/SpecMindDesktop/config/app.json`（不存在）
  3. 静默回退 mock：LLM 失败后 `except Exception: pass` + 用硬编码 mock 数据，用户完全不知道 LLM 没工作
- **修复方案**：
  1. spec 添加 `collect_data_files('certifi')` + openai/httpx hiddenimports + SSL 证书数据
  2. state.py 新增 `llm_errors: Annotated[List[str], operator.add]` 字段（并行安全累加）
  3. mock_agents.py 3 个 Agent（PM/Review/Planner）LLM 失败不再静默，错误信息写入 state.llm_errors + mock 数据首行标注 `⚠ LLM 调用失败`
  4. orchestrator.py 工作流完成后检查 llm_errors 并发警告日志到 GUI
- **是否成功**：✅ 成功（语法验证通过 + exe 构建 130.52 MB）
- **涉及文件**：SpecMindDesktop.spec, src/agents/state.py, src/agents/mock_agents.py, src/core/orchestrator.py
- **日期**：2026-07-14
- **备注**：人工测试发现迭代。PyInstaller 打包 LLM 应用必须包含 certifi 证书；LLM 失败回退必须显式标注，禁止静默回退
